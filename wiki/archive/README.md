# Archive

Store documents that were once useful but must not be treated as descriptions of the current system.

Archived or superseded documents should have non-current `status` metadata. Prefer preserving historically meaningful material over leaving ambiguous obsolete documents in active folders.

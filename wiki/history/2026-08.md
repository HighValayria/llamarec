---
title: "Semantic History 2026-08"
type: history
status: current
authority: descriptive
source: mixed
created: 2026-08-01
updated: 2026-08-03
last_verified: 2026-08-03
related_code:
  - src/analysis/threshold_comparison.py
  - src/analysis/summarize_results.py
  - src/analysis/basic_error_analysis.py
  - src/analysis/README.md
  - configs/experiment.yaml
  - src/data/preprocess.py
  - src/inference/base_zero_shot.py
  - src/inference/evaluate_y_adapter.py
  - src/inference/evaluate_n_adapter.py
  - src/inference/evaluate_m_adapter.py
  - src/inference/prediction_io.py
  - src/inference/prompts.py
  - src/inference/scoring.py
  - src/inference/README.md
  - src/train/preference_dataset.py
  - src/train/next_item_dataset.py
  - src/train/multitask_dataset.py
  - src/train/train_y.py
  - src/train/train_n.py
  - src/train/train_m.py
  - src/train/README.md
  - configs/y.yaml
  - configs/n.yaml
  - configs/m.yaml
  - tests/test_movielens_1m_preprocess.py
  - tests/test_analysis_outputs.py
  - tests/test_base_zero_shot_local.py
  - tests/test_y_adapter_evaluation.py
  - tests/test_n_m_adapter_evaluation.py
  - tests/test_y_training_data.py
  - tests/test_n_training_data.py
  - tests/test_m_training_data.py
  - wiki/current_state.md
  - wiki/index.md
  - wiki/reports/movielens_1m_mvp_results.md
  - wiki/reports/m_multitask_interference_diagnosis_results.md
  - wiki/reports/mvp_execution_status_and_findings.md
  - wiki/reports/phase_1_5_step_a_repository_check.md
  - wiki/modules/inference_layer.md
  - wiki/modules/training_layer.md
  - wiki/guides/current_data_flow.md
  - wiki/guides/project_walkthrough_for_learning.md
  - wiki/guides/step4_base_zero_shot_guide.md
---

# 2026-08

## 2026-08-03 - 建立 Phase 1.5 STEP B 阈值比较入口

What changed:

- 新增 `src/analysis/threshold_comparison.py`，读取 Base/Y/M 的 Y-task prediction 文件，并统一输出 AUC、固定 0.5 threshold、validation-calibrated threshold 三张二分类口径表。
- 更新 `tests/test_analysis_outputs.py`，用 toy prediction 验证三张表、validation 阈值选择和 test 应用逻辑。
- 更新 `src/analysis/README.md` 与 `wiki/current_state.md`，记录 STEP B 的运行方式和“正式报告需在完整云端 prediction 环境中生成”的限制。

Why:

- Phase 1.5 要先统一二分类指标口径，避免把 threshold-free AUC、默认 0.5 threshold F1、validation 校准后的 F1 混在一起解释。

Impact:

- 云端同步代码后，可直接运行 `python -m src.analysis.threshold_comparison` 为 Base/Y/M0/M1/M2 生成正式 STEP B 报告。
- 本次改动不改变模型、数据切分、candidate set 或已有 prediction 文件。

Affected code/docs:

- `src/analysis/threshold_comparison.py`
- `src/analysis/README.md`
- `tests/test_analysis_outputs.py`
- `wiki/current_state.md`
- `wiki/history/2026-08.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-03 - 进入 Phase 1.5 并完成 STEP A 仓库检查

What changed:

- 新增 `wiki/reports/phase_1_5_step_a_repository_check.md`，记录 prediction schema、候选集生成逻辑、动态 candidate_num 支持、tokenizer 标签风险、阈值校准代码和结果汇总代码的检查结论。
- 更新 `README.md`、`task.md`、`wiki/current_state.md`、`wiki/index.md` 和 `wiki/reports/README.md`，将项目当前阶段标记为 Phase 1.5。

Why:

- MVP 主链路和 M0/M1/M2 诊断已经完成，下一阶段应优先统一实验口径、做分组诊断和 ranking 稳健性验证，而不是继续训练新模型。

Impact:

- 发现当前本地仓库没有 MovieLens-1M 全量云端 prediction 文件，正式分组分析需要拉回云端 outputs 或在云端运行新增分析代码。
- 发现 prediction 文件缺少 target rating、timestamp、history length、popularity 等分组字段，后续需要离线 metadata enrichment。
- 发现 k=20/50 不能只改配置，需扩展候选集输出路径、ranking 指标和 tokenizer label 检查。

Affected code/docs:

- `README.md`
- `task.md`
- `wiki/current_state.md`
- `wiki/index.md`
- `wiki/reports/README.md`
- `wiki/reports/phase_1_5_step_a_repository_check.md`
- `wiki/history/2026-08.md`

Commit / reference:

- 文档同步，代码行为未变。

## 2026-08-03 - 新增 MVP 执行总览报告

What changed:

- 新增 `wiki/reports/mvp_execution_status_and_findings.md`，汇总任务迁移、STEP 1-8 执行历程、MovieLens-1M 当前状态、M0/M1/M2 诊断和主要发现。
- 更新 `wiki/index.md`、`wiki/reports/README.md` 和 `wiki/current_state.md`，将该总览报告加入当前报告入口。

Why:

- 当前主结果、M 诊断和阈值校准已经分散在多份报告中，需要一份面向后续接手者的总览文件，说明项目如何走到现在、目前停在哪、哪些发现已经成立。

Impact:

- 后续写实验报告或决定 Phase 2 时，可以先阅读该总览报告，再进入主结果报告和 M 诊断结果。
- 本次仅同步文档，不改变代码、数据划分、训练配置或实验结果。

Affected code/docs:

- `wiki/reports/mvp_execution_status_and_findings.md`
- `wiki/index.md`
- `wiki/reports/README.md`
- `wiki/current_state.md`
- `wiki/history/2026-08.md`

Commit / reference:

- 文档同步，代码行为未变。

## 2026-08-03 - 固化 M0/M1/M2 干扰诊断结论

What changed:

- 新增 `wiki/reports/m_multitask_interference_diagnosis_results.md`，记录 M0/M1/M2 的默认阈值结果、threshold calibration、binary error analysis 和 ranking 诊断。
- 将 `wiki/reports/m_multitask_interference_diagnosis_plan.md` 标记为 `superseded`，并从 active index 中替换为结果报告。
- 更新 `task.md`、`README.md`、`wiki/current_state.md`、`wiki/architecture/mvp_experiment_contract.md` 和 `wiki/reports/movielens_1m_mvp_results.md`，明确当前不启动 M3。

Why:

- 用户已经完成 M1、M2 和 threshold calibration。结果显示 M1 是当前最佳 M 诊断版本，M2 的 `Y:N=2:1` 没有缓解干扰且损害 N。

Impact:

- M1 在 validation best-F1 threshold 0.3208213008 下，test AUC=0.7669、F1=0.7818、Accuracy=0.7029，接近 Y-K0。
- M1 的 test HR@1=0.6950，明显优于 M0 但仍低于 N-K0。
- 后续优先做分组 error analysis 和报告整理，而不是继续长训练或进入 Phase 2 模块。

Affected code/docs:

- `task.md`
- `README.md`
- `wiki/current_state.md`
- `wiki/index.md`
- `wiki/architecture/mvp_experiment_contract.md`
- `wiki/reports/movielens_1m_mvp_results.md`
- `wiki/reports/m_multitask_interference_diagnosis_plan.md`
- `wiki/reports/m_multitask_interference_diagnosis_results.md`
- `wiki/reports/README.md`

Commit / reference:

- 文档同步，代码行为未变。

## 2026-08-03 - 新增 M-Y threshold calibration 分析

What changed:

- 新增 `src/analysis/threshold_calibration.py`，用 validation 选择 Yes/No best-F1 threshold，再将同一 threshold 应用到 test。
- 支持同时比较 Base、Y-K0 与多个 M runs，例如 M0/M1/M2。
- 新增测试覆盖 validation threshold 选择、test 应用、CSV/Markdown 输出。
- 更新分析模块说明、当前状态和 M 多任务干扰诊断文档。

Why:

- M1 的 AUC 已接近 Y-K0，但默认 0.5 threshold 下 F1 下降且 FN 上升，需要区分“排序能力不足”和“概率校准/阈值偏移”。

Impact:

- 该工具已经用于 M0/M1/M2 诊断。校准结果显示 M1 的 binary 能力接近 Y-K0，默认 0.5 threshold 低估了 M1。
- 当前仍不进入 KAR、Hard Negative、SASRec、7B、多 seed 或 32M full training。

Affected code/docs:

- `src/analysis/threshold_calibration.py`
- `tests/test_analysis_outputs.py`
- `src/analysis/README.md`
- `wiki/current_state.md`
- `wiki/reports/m_multitask_interference_diagnosis_plan.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-02 - 补齐 M 诊断训练入口

What changed:

- `MultitaskTrainingDataset` 支持可配置 Y/N 采样比例，默认仍为 1:1，M2 可使用 2:1，M3 可使用 1:2。
- `train_m.py` 新增 `--task-ratio-y` 与 `--task-ratio-n`，并在 mixed validation 外追加 Y-only 与 N-only validation loss。
- M 训练的 `run_summary.json` 与 `encoded_dataset_summary.json` 会记录实际 ratio、实际交替样本数和 Y/N task counts。
- 新增测试覆盖 1:1、2:1、1:2、非法 ratio、CLI 覆盖配置和 per-task validation 触发条件。

Why:

- MovieLens-1M 主结果显示 M-K0 低于对应单任务模型，下一阶段需要诊断是否为未收敛、任务失衡或更深层的任务冲突。

Impact:

- M1 可以通过 `--max-steps 3000` 运行。
- M2 可以通过 `--task-ratio-y 2 --task-ratio-n 1` 运行。
- 启动长训练前仍建议先跑小样本 smoke，确认 `eval_y_loss`、`eval_n_loss` 和 adapter reload check 正常。

Affected code/docs:

- `configs/m.yaml`
- `src/train/multitask_dataset.py`
- `src/train/train_m.py`
- `src/train/README.md`
- `tests/test_m_training_data.py`
- `task.md`
- `README.md`
- `configs/README.md`
- `wiki/current_state.md`
- `wiki/architecture/mvp_experiment_contract.md`
- `wiki/modules/training_layer.md`
- `wiki/reports/m_multitask_interference_diagnosis_plan.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-02 - MVP 完成后转入 M 多任务干扰诊断

What changed:

- 更新任务书、README、current state、MVP 实验契约、训练层说明和 1M 主结果报告，将当前状态从“验证 Y/N/M 是否有效”调整为“诊断并缓解 Y/N 多任务干扰”。
- 新增 `wiki/reports/m_multitask_interference_diagnosis_plan.md`，定义 M0/M1/M2/M3 最小诊断矩阵、指标表、代码支持情况和云端命令。
- 明确当前 M-K0 是能力折中，不是超过单任务的正迁移；记录 M-Y Yes 偏置与 candidate_num=5 的结论边界。

Why:

- MovieLens-1M Base/Y/N/M 主结果已经完成，Y-K0 与 N-K0 均显著优于 Base，但 M-K0 低于对应单任务模型。
- 下一步应优先解释多任务干扰，而不是进入 KAR、Hard Negative、SASRec、7B、多 seed 或 32M full training。

Impact:

- 原“实现 Y/N/M 主流程”任务关闭。
- M1 仅改 `--max-steps` 在入口层可配置，但正式长训练前需要补齐分任务 validation 记录。
- M2 的 `Y:N=2:1` 需要代码支持；当前 `optimizer_step_ratio` 配置不会改变实际 1:1 交替行为。

Affected code/docs:

- `task.md`
- `README.md`
- `configs/README.md`
- `wiki/current_state.md`
- `wiki/index.md`
- `wiki/architecture/mvp_experiment_contract.md`
- `wiki/modules/training_layer.md`
- `wiki/guides/mvp_development_steps.md`
- `wiki/reports/README.md`
- `wiki/reports/movielens_1m_mvp_results.md`
- `wiki/reports/m_multitask_interference_diagnosis_plan.md`

Commit / reference:

- 文档迁移与代码能力检查，未启动新的云端长训练。

## 2026-08-02 - 固化 MovieLens-1M MVP 主结果报告

What changed:

- 新增 `wiki/reports/movielens_1m_mvp_results.md`，记录 1M Base/Y/N/M 主结果、研究问题回答、基础 error analysis 和下一步建议。
- 更新 `wiki/index.md` 与 `wiki/current_state.md`，将 1M 主结果标记为已固化。

Why:

- 1M 的 `outputs/results.csv`、Markdown 主结果表和基础 error analysis 已在云端生成，需要将结论边界写入知识库，避免继续无目标扩展实验。

Impact:

- 当前 MVP 主结果可从 Wiki 直接复原。
- 下一步应优先写实验报告正文或只做一个 M 诊断实验，不进入 Phase 2 扩展。

Affected code/docs:

- `wiki/reports/movielens_1m_mvp_results.md`
- `wiki/index.md`
- `wiki/current_state.md`

Commit / reference:

- 文档同步，未计划作为项目代码提交。

## 2026-08-02 - 建立 STEP 8 结果汇总与基础错误分析

What changed:

- 新增 `summarize_results.py`，读取 Base/Y/N/M metrics 并生成 `outputs/results.csv` 与 Markdown 主结果表。
- 新增 `basic_error_analysis.py`，读取 prediction JSONL 并输出 binary confusion、ranking rank/margin/position sanity check 和代表性错误样本。
- 新增 STEP 8 分析脚本测试。

Why:

- MovieLens-1M Base/Y/N/M 预算实验已跑通，当前需要固化主结果，而不是继续盲目训练或增加 Phase 2 模块。

Impact:

- 云端可直接生成结构化结果表和基础 error analysis 文件。
- 当前分析仍限制在 MVP 范围，不加入 KAR、Hard Negative、多 seed 或额外模型。

Affected code/docs:

- `src/analysis/summarize_results.py`
- `src/analysis/basic_error_analysis.py`
- `tests/test_analysis_outputs.py`
- `src/analysis/README.md`
- `wiki/current_state.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-02 - 主实验数据集调整为 MovieLens-1M

What changed:

- `configs/experiment.yaml` 新增 `movielens-1m` 原始文件定义，并将 `dataset.formal` 从 `movielens-32m` 调整为 `movielens-1m`。
- `preprocess.py` 新增 MovieLens-1M `ratings.dat` / `movies.dat` 的 `::` 格式读取。
- 新增 1M 原始格式读取测试。
- 本地已对 MovieLens-1M 完成 STEP 2 与 STEP 3，生成 Y/N 样本和固定 candidate set。

Why:

- MovieLens-100K 已完成 Base/Y/N/M 预算实验，验证了 MVP 全链路和核心研究问题方向。
- MovieLens-32M 对当前 MVP 训练与评测成本过高，不适合作为当前主线。

Impact:

- 后续主实验优先在 MovieLens-1M 上复现 Base/Y/N/M。
- MovieLens-32M 暂停作为 Phase 2 或压力测试数据，不再作为当前 MVP 的默认 formal dataset。

Affected code/docs:

- `configs/experiment.yaml`
- `src/data/preprocess.py`
- `tests/test_movielens_1m_preprocess.py`
- `task.md`
- `README.md`
- `configs/README.md`
- `wiki/current_state.md`
- `wiki/architecture/mvp_experiment_contract.md`
- `wiki/modules/movielens_data_layer.md`
- `wiki/guides/current_data_flow.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 新增 N-K0 与 M-K0 adapter 评测入口

What changed:

- 新增 `evaluate_n_adapter.py`，可加载 base model + N-K0 PEFT adapter，对固定 N candidate set 输出 `P(A)...P(E)`、prediction JSONL 与 Ranking metrics。
- 新增 `evaluate_m_adapter.py`，可加载 base model + M-K0 PEFT adapter，分别评测 M-Y 的 `P(Yes)/P(No)` 与 M-N 的 `P(A)...P(E)`。
- 新增 N/M adapter 评测测试，覆盖 mock 模式 prediction 文件、metrics 文件和两个 M 推理接口。

Why:

- N tiny smoke 与 M tiny smoke 已在云端跑通；在启动更长训练前，需要先具备统一 adapter 评测入口，便于每个训练产物自动输出可比较指标。

Impact:

- 云端可在新 SSH 中对 `outputs/n/.../adapter` 与 `outputs/m/.../adapter` 先跑小样本真实评测。
- M-K0 评测结果会同时保留 M-Y 与 M-N，不把其中一个接口替代为 M 的唯一结果。

Affected code/docs:

- `src/inference/evaluate_n_adapter.py`
- `src/inference/evaluate_m_adapter.py`
- `tests/test_n_m_adapter_evaluation.py`
- `wiki/current_state.md`
- `wiki/modules/inference_layer.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 修复 M-K0 SequentialSampler 的 Trainer 版本兼容

What changed:

- `train_m.py` 的 `SequentialTrainer._get_train_sampler()` 现在兼容 transformers 传入 `train_dataset` 参数的版本。
- 新增测试覆盖“传入 dataset / 使用 fallback dataset”两种路径。

Why:

- 云端 M tiny smoke 启动训练时，当前 transformers 版本调用 `_get_train_sampler(dataset)`，旧实现只接受 `self`，导致 `TypeError`。

Impact:

- M tiny smoke 可在云端拉取修复后重跑。
- 本次修复不改变 M 的 Y/N 交替训练语义。

Affected code/docs:

- `src/train/train_m.py`
- `tests/test_m_training_data.py`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 新增 N-K0 与 M-K0 训练入口

What changed:

- 新增 `src/train/next_item_dataset.py`，将 N 样本编码为 candidate label 训练样本，只在 `A-E` 答案 token 上计算 loss。
- 新增 `src/train/train_n.py`，提供 N-K0 QLoRA 训练入口、checkpoint、validation、adapter 保存和 `P(A)...P(E)` reload 检查。
- 新增 `src/train/multitask_dataset.py`，将 Y 与 N 样本按 1:1 顺序交替编码。
- 新增 `src/train/train_m.py`，提供 M-K0 QLoRA 多任务训练入口，并在 reload 检查中分别验证 M-Y 与 M-N 两个接口。
- 更新 `configs/n.yaml` 与 `configs/m.yaml` 为 smoke training ready。
- 新增 N/M 训练数据编码测试。

Why:

- Y-K0 已经完成 smoke 训练、adapter reload 和小样本真实评测；等待 Y 预算版训练期间可以推进 STEP 6/7 代码准备。
- 当前仍坚持 MVP 范围，不加入 KAR、Hard Negative、多 seed 或其他 Phase 2 功能。

Impact:

- 云端可在当前 Y 训练结束后自动排队运行 N smoke / N 预算训练 / M smoke。
- N/M 目前只完成本地编码测试，尚未完成云端真实模型 smoke，因此不能视作实验已完成。

Affected code/docs:

- `configs/n.yaml`
- `configs/m.yaml`
- `src/train/next_item_dataset.py`
- `src/train/multitask_dataset.py`
- `src/train/train_n.py`
- `src/train/train_m.py`
- `tests/test_n_training_data.py`
- `tests/test_m_training_data.py`
- `wiki/current_state.md`
- `wiki/modules/training_layer.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 修复 Y adapter 评测的 PEFT wrapper logits 兼容问题

What changed:

- `RealModelScorer._last_token_logits()` 现在兼容 wrapped model 输出 `logits` 的情况。
- 当模型内部输出 `last_hidden_state` 时仍走原来的 `lm_head` 路径；当 PEFT wrapper 输出 `logits` 时直接选取最后有效 token 位置的 logits。
- 新增本地测试复现 wrapped model 返回 logits 的路径。

Why:

- 云端运行 Y-K0 adapter 小样本评测时，`PeftModel.model(...)` 返回 `CausalLMOutputWithPast`，其中包含 `logits` 但没有 `last_hidden_state`，旧代码因此触发 `AttributeError`。

Impact:

- Y adapter 真实评测可继续使用相同命令重跑。
- 本次修复不改变 prompt、数据划分、候选集或指标定义。

Affected code/docs:

- `src/inference/scoring.py`
- `tests/test_base_zero_shot_local.py`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 新增 Y-K0 adapter 评测入口

What changed:

- 新增 `src/inference/evaluate_y_adapter.py`，提供独立 Y adapter 评测 CLI。
- 新增 `AdapterModelScorer`，可在云端加载 base model + PEFT adapter 并输出 `P(Yes)/P(No)`。
- Y adapter 的 N ranking 评测读取固定 N candidate set，但对每个 candidate 单独构造 Y prompt，并使用 `P(Yes)` 作为候选分数。
- 新增本地 mock 测试，覆盖 prediction 输出、metrics 输出和 N-by-Y candidate scoring schema。

Why:

- Y-K0 smoke training 与 adapter reload check 已跑通，下一步需要按 Base 相同的 validation/test 口径评测 Y adapter。
- Y-K0 没有学习 A-E candidate label，因此它用于 ranking 时必须通过 Yes/No 接口逐候选打分，而不是读取 `P(A)...P(E)`。

Impact:

- 云端可先对 100K smoke adapter 做小样本真实评测，再对 32M smoke/formal adapter 评测。
- 本次改动不改变数据划分、固定候选集或指标定义。

Affected code/docs:

- `src/inference/evaluate_y_adapter.py`
- `src/inference/scoring.py`
- `src/inference/prompts.py`
- `tests/test_y_adapter_evaluation.py`
- `wiki/current_state.md`
- `wiki/modules/inference_layer.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 添加面向学习的工程讲解并同步当前状态文档

What changed:

- 新增 `wiki/guides/project_walkthrough_for_learning.md`，按 STEP 1-8 和 Base/Y/N/M 功能轴解释当前工程。
- 将新学习指南加入 `wiki/index.md`。
- 同步 `README.md`、`wiki/guides/current_data_flow.md` 和 `wiki/modules/inference_layer.md` 中关于 STEP 4 / STEP 5 当前状态的描述。

Why:

- 项目已经推进到数据层、评测层、推理层和 Y-K0 训练层并存，对学习者而言需要一份从推进步骤到功能模块的当前讲解。
- 活跃文档中仍有旧描述称真实 Base 推理和 Y-K0 尚未完成，已与 `wiki/current_state.md` 和当前实现不一致。

Impact:

- 新读者可以先读学习指南，再进入具体源码。
- 文档状态更一致：当前仍未完成的是结果汇总、Y-K0 正式训练/评测、Y reload 补验、N-K0、M-K0 和 error analysis。

Affected code/docs:

- `README.md`
- `wiki/index.md`
- `wiki/guides/current_data_flow.md`
- `wiki/guides/project_walkthrough_for_learning.md`
- `wiki/modules/inference_layer.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 修复 Y-K0 reload check 的 tokenizer 输出兼容问题

What changed:

- `src/train/train_y.py` 的 reload check 现在兼容 tokenizer 返回 `dict`、带 `input_ids` 属性对象、单条 batch 列表、tensor/list 等形式。
- 新增 `--reload-only` 与 `--adapter-dir` 参数，可在不重新训练的情况下检查已保存 Y adapter 是否能重新加载并输出 `P(Yes)/P(No)`。
- 新增本地测试覆盖 Y reload token id 规范化逻辑。

Why:

- 云端 Y-K0 MovieLens-100K smoke training 已完成，LoRA 注入、loss 下降、validation loss 和 adapter 保存均正常；失败发生在训练后的可选 reload check。
- 当前环境中 chat 格式化返回了 `{"input_ids": ...}`，旧代码把字典 key 当作 token id 转换，触发 `ValueError: invalid literal for int() with base 10: 'input_ids'`。

Impact:

- 该问题不影响已经完成的训练过程和 adapter 保存。
- 云端同步代码后，可直接用 `--reload-only --adapter-dir ...` 补跑 adapter 加载与 `P(Yes)/P(No)` 输出检查。
- 本次修复不改变 Y/N/M 任务定义、数据划分、候选集或评测指标。

Affected code/docs:

- `src/train/train_y.py`
- `tests/test_y_training_data.py`
- `wiki/current_state.md`
- `wiki/modules/training_layer.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 建立 STEP 5 Y-K0 训练代码链路

What changed:

- 新增 `src/train/preference_dataset.py`，将 Y 偏好样本编码为 causal LM 训练样本，并只在 `Yes/No` 答案 token 上计算 loss。
- 新增 `src/train/train_y.py`，提供 Y-K0 训练 CLI，支持 QLoRA、checkpoint、validation、adapter 保存和可选重载检查。
- 更新 `configs/y.yaml`，标记 Y-K0 为 smoke training ready，并记录训练入口与 smoke 默认参数。
- 新增训练层 Wiki 文档和本地测试。

Why:

- STEP 4 Base 全量评测正在云端运行。等待期间可以先准备 STEP 5 的 Y-K0 smoke/overfit 代码，但不能跳过 Base 结果检查直接做结论。

Impact:

- Base 全量结果检查通过后，可在云端运行 Y-K0 smoke/overfit。
- 当前只实现 Y-K0；N-K0 和 M-K0 仍未实现。
- MovieLens-32M 正式 Y 训练前仍需要确认完整 `preference_train.jsonl.gz` 已在云端生成。

Affected code/docs:

- `configs/y.yaml`
- `src/train/preference_dataset.py`
- `src/train/train_y.py`
- `src/train/README.md`
- `tests/test_y_training_data.py`
- `wiki/index.md`
- `wiki/current_state.md`
- `wiki/modules/training_layer.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 为 Base zero-shot 接入 batch 推理与进度输出

What changed:

- `base_zero_shot.py` 新增 `--batch-size`，并兼容用户误输入的 `--batch_size`。
- Y 和 N prediction 改为按 batch 增量写出，避免全量运行结束前一直没有输出文件。
- real scorer 新增批量单 token logits 推理路径；对于 Llama 当前的 Yes/No/A-E 单 token 答案，可一次 forward 多条 prompt。
- 增加 tqdm 进度条；没有安装 tqdm 时自动退回普通循环。
- 本地测试覆盖 mock batch scoring 与 batch size 入口。

Why:

- MovieLens-32M Base validation/test 接近百万次打分。逐条 forward 在 4090 上预计需要数小时，且中途缺少进度反馈。

Impact:

- 云端可先运行 `--limit 5000 --batch-size 16` 估算吞吐，再执行全量 `validation test`。
- 本次改动不改变 Y/N 任务定义、候选集、指标或数据划分；只改变推理执行方式和输出写入节奏。

Affected code/docs:

- `src/inference/base_zero_shot.py`
- `src/inference/scoring.py`
- `src/inference/prediction_io.py`
- `src/inference/README.md`
- `tests/test_base_zero_shot_local.py`
- `wiki/current_state.md`
- `wiki/modules/inference_layer.md`
- `wiki/guides/step4_base_zero_shot_guide.md`

Commit / reference:

- 当前工作区修改，尚未提交。

## 2026-08-01 - 修复 real scorer 的 tokenizer 输出兼容问题

What changed:

- `RealModelScorer` 增加 tokenizer 输出规范化逻辑，兼容 chat 格式化返回字符串、`list[int]`、单条 batch 形式列表或 tensor 的情况。
- 将真实模型加载参数从 `torch_dtype` 更新为 `dtype`，消除新版 `transformers` 的弃用警告。
- 增加本地测试覆盖 tokenizer 输出规范化。

Why:

- 云端运行 `--mode real` 时，模型权重已成功加载，但 tokenizer 的 chat 格式化结果在当前环境中返回字符串，导致 `torch.tensor()` 收到字符串并报 `ValueError: too many dimensions 'str'`。

Impact:

- 云端同步 `src/inference/scoring.py` 后，可重新运行 Base real 小样本验证。
- 本次修复不改变 Y/N 任务语义、候选集、指标或数据划分。

Affected code/docs:

- `src/inference/scoring.py`
- `tests/test_base_zero_shot_local.py`
- `wiki/index.md`
- `wiki/history/2026-08.md`

Commit / reference:

- 当前工作区修改，尚未提交。
